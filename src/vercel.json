{
    "version": 2,
    "builds": [
      { "src": "package.json", "use": "@vercel/node" }
    ],
    "env": {
      "PG_CONFIG": "/usr/local/bin/pg_config"
    },
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/src"
      }
    ]
  }
  